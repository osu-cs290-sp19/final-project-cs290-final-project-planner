<article class="list">
  <div class="listContent">
    <p class="listName">
      {{listName}}
    </p>
    <div class="makeList">
        <ul>
            {{#each listItems}}
              {{> createListItem}}
            {{/each}}
        </ul>
    </div>
  </div>
</article>
