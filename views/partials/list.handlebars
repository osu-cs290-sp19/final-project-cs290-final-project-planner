<article class="list">
  <div class="listContent">
    <p class="listName">
      {{listName}}
    </p>
    <p class="makeList">
      {{#each newItem}}
        {{> createListItem}}
      {{/each}}
    </p>
  </div>
</article>
